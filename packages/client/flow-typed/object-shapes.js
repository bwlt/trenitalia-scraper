declare type SolutionObject = {
  origin: string,
  destination: string,
  arrivaltime: string,
  departuretime: string,
  minprice: string,
};

declare type StationObject = {
  name: string,
  id: string
}
